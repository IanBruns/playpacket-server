CREATE TABLE rules (
    rulesId INTEGER PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
    game_id INTEGER REFERENCES games(gamesId) ON DELETE CASCADE NOT NULL,
    rule_title TEXT,
    rule_description TEXT NOT NULL,
    assigned_user INTEGER REFERENCES users(usersId) ON DELETE CASCADE NOT NULL
);